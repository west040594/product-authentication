spring:
  application:
    name: authentication-system
  cloud:
    consul:
      enabled: false
      discovery:
        enabled: false
        instance-id: ${spring.application.name}
        health-check-interval: 15s